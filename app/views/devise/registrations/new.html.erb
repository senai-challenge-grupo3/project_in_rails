<div class="devise">
	<h2>Sign up</h2>

	<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
	  <%= f.error_notification %>

	  <div class="form-inputs">
	    <%= f.input :email, required: true, label:false, placeholder: "E-mail", autofocus: true %>
	    <%= f.input :password, required: true, label:false,  placeholder: "Senha" %>
	    <%= f.input :password_confirmation, label:false, required: true, placeholder: "Confirmação de senha" %>
	  </div>

	  <div class="form-actions">
	    <%= f.button :submit, "Cadastrar" %>
	  </div>
	<% end %>

	<%= render "devise/shared/links" %>
</div>
